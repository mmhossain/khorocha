import { Box } from "@chakra-ui/react";

const FilterBar = () => {
  return (
    <Box p={4} borderWidth="1px" borderRadius="lg" mb={4}>
      FilterBar Component
    </Box>
  );
};

export default FilterBar;
