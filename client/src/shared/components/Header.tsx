import { Box, Heading } from "@chakra-ui/react";

const Header = () => {
  return (
    <Box bg="teal.500" color="white" p={4}>
      <Heading size="lg">Khorocha - Expense Tracker</Heading>
    </Box>
  );
};

export default Header;
